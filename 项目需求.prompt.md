我需要做一个用于Counter-Strike 2的地图Ban Pick工具
该项目需要在公网环境下打开且可使用浏览器直接访问界面
在BP开始前 需要由管理员在后台创建BP房间 并生成唯一房间码 管理员之外的账号无需登录(管理员登录的方式位于不起眼的位置 如右下角)
管理员可以创建BP房间 并设置地图池(可从预设地图池中选择或自定义地图池 确保拥有7张地图 与Counter-Strike 2 Primier模式中的BP相似 且每一张地图的名称和图标可以自定义 以防止游戏更新导致地图名称和图标变化变化以及Valve更新游戏导致的地图池轮换)
管理员创建房间后 会生成唯一房间码 供其他用户加入房间使用
管理员需要设置双方队伍和图标名称
房间创建后 其他用户可以通过房间码加入房间 并选择自己所属的队伍(展示两个队伍名称和图标 供用户选择)
加入房间后 在BP开始前 需要用户输入自己在所在队伍中的名称 以便管理员识别
在BP开始前 需要所有用户确认自己所在队伍和名称 无误后方可开始BP 以及需要10个用户同时准备 BP才会开始
在BP过程中 需要按照Counter-Strike 2的BP规则进行地图Ban Pick
队伍中的5个人在网页中的左侧展示(队员名称) 左下可以输入文字以在BP时进行简便的文字交流 双方交流内容不互通 且聊天框内的格式应为"[Team]Player:Content" 右侧是敌方队伍的5个人展示(队员名称) 两侧顶上有队伍的名称及图标 BP界面位于网页正中央
在用户进行地图Ban Pick时 需要有明显的提示当前轮次和操作人 以及当前可选地图和已被Ban的地图
网页正中间的BP界面下方 需要有一个计时器 显示当前操作剩余时间(每一个操作15s) 超时则自动跳过该用户的操作 并进入下一轮 如果平票 则随机选择一个地图进行Ban或Pick
网页正中间需要有一个正七边形展示当前地图池状态 该正七边形的底部应为底边 顶部为该正七边形的一个顶点 地图的变量名称从该正七边形的顶点开始顺时针命名(map01~map07) 管理员可设置map01~map07的所有属性(在全局设置中) 地图池是管理员在后台设置的7张地图 在这个正七边形的每一个顶点上需要放置一个方框 每一个方框放置一个地图 在方框底下 需要有四个字的加粗白色地地图名字始终保持(管理员选择的地图的名字) 已被Ban的地图以灰色(降低亮度)显示并打上红色方框并展示白色的禁用文字 当前被Pick的地图以高亮显示并打上绿色方框其中展示白色的√ 每个已被选择的地图方框的顶部高亮显示"1st/2nd - PICKED BY [Team]"/"3rd - DECIDER" 剩余可选地图以正常颜色显示
在该轮次进行时 每一个地图的中间 需要有一块用虚线标出的亮度变为20%的区域 类似"回"字形结构 以提示用户当前可选地图 整个方框皆可点击 但是红色的×和绿色的√需要显示在该虚线方框内 以提示用户该地图的状态 在每一个地图方框的左侧 显示已进行操作的队友的名字(每一个用户输入的名称 当敌方进行操作时则不显示) 以便提示当前操作人队友的意向和队伍选图趋势
该项目主要用于BO3的BP模式 因此需要按照BO3的BP规则进行设计 
在所有人准备完毕后 BP会自动开始 并按照以下顺序进行:
1. 在所有人以及队伍确认准备完毕后 由双方进行Roll点 决定先后手 点数较大的作为先手队伍进行第一次Ban(0~99随机数)
2. 第一轮Ban: 每个队伍依次Ban一张地图 (共2张地图被Ban)
3. 第二轮Ban: 每个队伍依次Ban一张地图 (共2张地图被Ban)
4. 一轮Pick: 每个队伍依次Pick一张地图 (共2张地图被Pick)
5. 最终决胜图Decider: 自动选择剩余的最后一张地图作为决胜图
在BP的过程中需要模仿Counter-Strike 2的Primier模式的BP界面风格 以提升用户体验 顶部需要有一个进度条 显示当前BP进度 进度条分成七段 每完成一个Ban或Pick 进度条增加一段 在每一段中 需要缓慢填充进度条(类似Primier模式中的动画效果)
七段进度条上方 需要有文字："禁用1张地图"/"禁用1张地图"/"选择1张地图"/"选择1张地图"/"禁用1张地图"/"禁用1张地图"/"决胜图展示" 且只有当前进行的操作对应的文字为高亮显示 其他文字为灰色显示 顶部还需要展示改进度条填充完毕所需要的剩余秒数 仅需显示纯数字且高亮展示 当该阶段不再进行或尚未开始进行时 隐藏该数字
在这个正七边形的顶点上方 也需要展示文字
"禁用"(红色)"0/1"(白色)"地图"(红色)
"选择"(绿色)"0/1"(白色)"地图"(绿色)
"等对方禁用/选择1张地图"(白色)
"决胜图"(黄色)
在该轮次时分别在各用户的界面上显示对应合适的文字
在BP结束后 需要展示最终的BP结果并保持不变直到用户退出房间(此时该操作被允许)或关闭网页 包括被Ban的地图和Pick的地图 以及决胜图(按照被Ban和Pick的顺序进行展示) 将位于网页正中央的一个长方形切成七份 在每一份长方形的内部填充地图图片(等比例放大 剩余的部分裁切丢弃) 每一个长方形的下方都需要显示对应的地图名称 前两个长方形亮度降低至50%并在顶部写上"BANED BY [Team]" 中间两个长方形普通展示并在顶部写上"PICKED BY [Team]" 后续两个长方形亮度降低至50%并在顶部写上"BANED BY [Team]" 最后一个长方形普通展示并在顶部写上"DECIDER" 并在顶部的两边展示每一个队伍的名称和图标 以及双方队伍的Roll点结果 以便用户回顾
管理员可以在后台查看每一个房间的BP记录 包括每一个用户的操作日志 以及最终的BP结果 以便进行回放和分析
需求描述完毕